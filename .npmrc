# Supply chain hardening: refuse to resolve package versions published less
# than 7 days ago. Most malicious releases are yanked within hours or days.
# Requires npm >= 11.10 (older npm ignores this with a warning).
# See docs/sessions/2026-07-17-dependency-migration-plan.md (Phase 0).
min-release-age=7
