import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Code + Brews',
};

const CodeAndBrews = () => {
  return (
    <h1>Code + Brews</h1>
  );
};

export default CodeAndBrews;
