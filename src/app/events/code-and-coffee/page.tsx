import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Code & Coffee',
};

const CodeAndCoffee = () => {
  return (
    <h1>Code & Coffee</h1>
  );
};

export default CodeAndCoffee;
