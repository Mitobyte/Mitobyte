import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Hack & Tell',
};

const HackAndTell = () => {
  return (
    <h1>Hack & Tell</h1>
  );
};

export default HackAndTell;
